# 安全策略

## ⚠️ 免责声明

本项目按"原样"提供，**不提供任何明示或暗示的保证**。使用本项目产生的所有安全风险、数据泄露、账单超支、合规问题等后果由使用者自行承担。

**部署前请务必完整阅读 [DISCLAIMER.md](DISCLAIMER.md)。**

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| v1.0.0 | ✅ 当前稳定版，接收安全报告 |
| < 1.0.0 | ❌ 不再维护 |

## 上报安全问题

如果您在本项目中发现安全问题，请通过以下方式私下告知我们：

- 在 GitHub 上创建 **Private Security Advisory**（推荐）：仓库页 → Security → Advisories → New draft security advisory
- 或发送邮件至：见 GitHub 仓库主页的官方联系方式

**请勿**在公开的 Issue 中披露安全漏洞。

我们会在收到报告后的 7 个工作日内回复确认。

## 安全最佳实践（部署前必读）

### 1. 强密码

- 管理员密码至少 **16 位**，包含大小写字母、数字、特殊字符
- 不要在多个系统使用相同密码
- 定期更换（建议每 90 天）

### 2. 访问控制

- 启用 **Cloudflare Access** 保护 `/admin/*` 路径（推荐）
- 限制管理后台的访问来源（IP 白名单）
- 启用 MFA（多因素认证）

### 3. API Key 保护

- 首次部署后**立即轮换**所有上游 API Key
- 在 Cloudflare KV 中存储的 API Key 虽做了简单保护，但 KV 数据可被 Cloudflare 账户管理员读取
- 为不同用户/项目使用不同的子 API Key
- 定期检查上游 API 平台的用量统计

### 4. 网络安全

- 启用 Cloudflare WAF 规则
- 设置 Rate Limiting 规则（Cloudflare 自动基础防护 + 自定义规则）
- 启用 Bot Fight Mode
- 考虑启用 Cloudflare Turnstile 验证

### 5. 数据保护

- 启用 Cloudflare D1 加密
- 定期备份 KV/D1/R2 数据到外部存储
- 设置合理的日志保留期限（默认无期限，建议 90 天）
- 评估您所在司法管辖区的隐私法规要求（GDPR/CCPA/个人信息保护法）

### 6. 监控

- 设置 Cloudflare 预算告警
- 使用 healthchecks.io 或类似工具监控端点可用性
- 监控异常调用模式（如某 IP 大量失败请求）

## 已知安全限制

请阅读 [DISCLAIMER.md](DISCLAIMER.md) 了解以下限制：

- 速率限制基于 IP，可被 IP 轮换绕过
- Cloudflare Workers 的 CPU 时间限制（10ms 免费版 / 30s+ 付费版）可能导致长请求被截断
- 文件上传走 R2 公开 URL，文件链接一旦泄露无法撤销
- 调用日志会记录 IP 地址，属于个人信息

## 合规性

本项目不提供以下合规功能，您需要自行实现：

- GDPR 数据删除接口
- 数据导出接口
- 审计日志（仅记录管理员操作）
- 用户同意管理

如需企业级合规功能，请基于本项目二次开发或考虑商业方案。

---

**最后更新：2026 年 9 月 6 日**
